/**
 * crear una funcion que recibe una
 * lista de numeros enteros positivos
 * y devuelva la lista son solo
 * numeros pares
 */

numeros = [45, 78, 564, 897, 345, 24, 48];

function soloPares(params) {
  //codigo
  var params = 0;
  for (i = 0; i <= numeros.lenght; i++) {
    params = numeros(parseInt);
  }
  if (params) {
    numeros(parseInt);
    return params;
  }
  
}

soloPares();
